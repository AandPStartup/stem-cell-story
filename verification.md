# Header Localization Verification

The Persian mode displays the header wordmark as «روایت سلول» with RTL-aware spacing and a Persian home label. After switching to English, the header restores the `cell story` wordmark, the English home label, and the left-to-right page layout. Both states rendered successfully with no TypeScript errors.

## Challenge detail verification

The live page loads after the rollback and the treatment-challenge section remains accessible in the existing layout. The challenge cards remain interactive; expanded detail content is ready for the new bilingual evidence line to be checked after selecting a card.

## Latest challenge-section verification

The page remains responsive after the challenge-content expansion. The interactive differentiation explorer and selected-station panel remain intact above the treatment-challenge section; no surrounding content was removed.
